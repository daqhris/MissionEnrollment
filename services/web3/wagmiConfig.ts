// This file is deprecated. Using app/config/wagmi.ts instead.
// Keeping this file temporarily to ensure no breaking changes during migration.
// TODO: Remove this file after confirming all imports are updated.

import { config } from "../../app/config/wagmi";
export const wagmiConfig = config;
